# AI Usage Notes

This implementation was built with Codex as a paired coding tool.

Prompt style used:

- Provided the full take-home brief and desired folder structure.
- Asked Codex to inspect the existing workspace before editing.
- Let Codex choose a focused implementation path that emphasized working must-haves over broad placeholders.

Review habits:

- Kept API-layer RBAC and object scoping as the primary acceptance criteria.
- Used smoke tests for login, denied access, scoped carrier loads, compliance blocking, load search, and rate endpoints.
- Kept notes in this file so AI assistance is visible in the repository as requested.
